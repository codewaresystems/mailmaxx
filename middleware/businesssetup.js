export default function ({$auth, redirect}) {
	// if (!$auth.user.business_info.business_name || !$auth.user.business_info.bussiness_type ) {
	// 	return redirect('/welcome')
	// }
	console.log('me')
}